# Terraform local cache and providers - regenerated by `terraform init`
.terraform/
.terraform.lock.hcl

# Terraform state - lives in GCS, never commit local copies
*.tfstate
*.tfstate.*

# Variable files that may contain secrets
*.tfvars
!example.tfvars

# Service account keys - never commit these
*.json
